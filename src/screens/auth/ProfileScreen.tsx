import React from 'react';
import { View, Text } from 'react-native';

const ProfileScreen: React.FC = () => (
  <View>
    <Text>ProfileScreen (Auth)</Text>
  </View>
);

export default ProfileScreen;

